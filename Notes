packages needed:
sudo pacman -S nerd-fonts
sudo pacman -S mpv
sudo pacman -S arandr
sudo pacman -S nitrogen
sudo pacman -S rofi
sudo pacman -S xscreensaver
sudo pacman -S feh
sudo pacman -S qtile
sudo pacman -S sddm
sudo pacman -S alacritty
sudo pacman -S conky
sudo pacman -S xdg-user-dirs

sudo pacman -S python-psutil
sudo pacman -S pacman-contrib

sudo pacman -S lsd

# get audio widget working
sudo pacman -S pavucontrol
sudo pacman -S pipewire-pulse

# for yay (AUR) and sublime-text
sudo pacman -S base-devel
sudo pacman -S git

for sddm
---------
theme dir: /usr/share/sddm/themes

make dir /etc/sddm.conf.d
copy /usr/lib/sddm/sddm.conf.d/default.conf to /etc/sddm.conf.d/default.conf.user
edit /etc/sddm.conf.d/default.conf
sudo pacman -S qt5-graphicaleffects qt5-quickcontrols2 qt5-svg
git clone https://aur.archlinux.org/sddm-sugar-dark.git
makepkg -si

feh_wallpaper.service needs to be stored at $HOME/.config/systemd/user
systemctl --user daemon-reload
systemctl --user enable
systemctl --user start



zsh 
====
https://github.com/romkatv/powerlevel10k#installation
sudo pacman -S zsh-syntax-highlighting
